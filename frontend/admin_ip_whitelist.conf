# 管理接口 IP 白名单（T2）
# 部署到公网前，取消注释并填写你的 VPN 网段或办公固定 IP：
#
# allow 10.0.0.0/8;      # 示例：Tailscale/WireGuard VPN 网段
# allow 203.0.113.10;    # 示例：办公固定 IP
#
# 默认未放行任何 IP，管理接口全部拒绝（由 nginx.conf 中的 deny all 兜底）。
